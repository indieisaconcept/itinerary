'use strict';

// Add new tests and associated fixtures as a name file within this
// directory or add related tests to an existing file.

module.exports = require('requireindex')(__dirname, 'simple inherited template helper file'.split(/[ ,]+/));
